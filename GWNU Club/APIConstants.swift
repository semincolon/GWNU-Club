import Foundation

struct APIConstants {
    static let baseURL = "http://localhost:8080"
    
    static let clubListURL = baseURL + "/clubs/ios"
    static let eventURL = baseURL + "/events/ios"
    
    static let imageURL = baseURL + "/images"
}
